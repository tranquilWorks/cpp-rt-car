# M23-02 CPU benchmark implementation evidence

Date: 2026-09-07. Baseline: `b52b42a6c64066553ce55fb08c874061d78e036e`
(merged M23-01 PR #251). Candidate branch: `agent/m23-02-cpu-benchmarks`.

## Status and provenance

Implementation candidate, not a completed milestone or qualification. The
canonical contract was integrated by portfolio-control PR #365 at
`0a2885232090c85620ff61aada1b0a5ee18d38ad`; active-batch binds its blob
`0dc164c3210266f1cdadb95b13203e9ccfe7f968`. The prior M23-01 initial Clang
failure and local full-profile dependency blockage remain unchanged historical
evidence. Repaired M23-01 head `cb136d671088ed01bc75b5be0c67d991e7814cdf`
passed CI run 34080242894 before merge. Merge does not invent human review.

Control PR #366 adds one omitted allowed path for the existing CLI artifact
regression. That regression assumes discovery contains only the self provider;
it remains unmodified pending integration of the correction. Control CI run
34083604699 failed before any validate job steps, with no job logs. No gate is
waived and no cause beyond the observed pre-step failure is inferred.

## Acceptance map

- Provider/CLI: explicit instance-local rtfw.cpu version 1; pure metadata;
  exact selection and destination validation before preparation. CPU-only
  example/CLI links Runtime; runner remains independent.
- Inventory: 51 finite cases covering thirteen contracted areas (memory and
  native residency use separate family labels), two warm-ups and five samples.
- Graph/executor: actual public graph, nested range/reduce, worker policy,
  bounded host queue admission/recovery, trace and completed-work observations.
- Correctness: independent integer oracle, exact per-index visits, compile
  order/dependency inspection and malformed cycle rejection.
- Memory/lifecycle: six-row accounting; simulated acquire/apply/observe and
  rollback/release counts; injected acquire/apply/one-retry rollback faults;
  inactive regions; opt-in unprivileged native residency; fresh Runtime cycles.
- Isolation/cleanup: concurrent independent owners and repeated runs; stopping
  one Runtime while the other continues; checked teardown before publication.
- Timing: lifecycle-inclusive compile/memory/lifecycle; full step-and-observation
  for prestarted fixtures. No subtraction, thresholds or performance claim.
- Packaging: installed example sources, default/unrequested component isolation,
  archive relocation and add-subdirectory consumption; validation in progress.
- Compatibility: Runtime, ABI, benchmark API/schemas/validator, support matrices,
  version/license and prior evidence unmodified; full gates in progress.

## Validation log (candidate worktree)

- GCC 13.3, CMake 4.4.3, Linux host. Configure `build/m23-cpu` with tests and
  benchmarks ON, experimental OFF, RelWithDebInfo, no sanitizers, WERROR ON:
  exit 0. Build focused targets: exit 0.
- `build/m23-cpu/tests/simcore_tests --gtest_filter=BenchmarkCpu.*`: exit 0,
  7/7 tests passed before the added malformed-ordinal assertion.
- `python3 tests/benchmark_fixtures/test_cpu_cases.py --cli
  build/m23-cpu/bench/rtfw-bench --inventory bench/fixtures/cpu_cases.json`:
  exit 0, all 51 cases validated, no native NOT RUN on this host. Includes
  fake clock exact counts, unchanged offline validator, collision preservation,
  invalid selection and a real-clock portable-characterization sample.
- `ctest --test-dir build/m23-cpu -R
  'm23_benchmark_cpu_noalloc|benchmark_cpu_package_consumer|m23_benchmark$'
  --output-on-failure`: exit 0, 3/3 gates passed. Allocation checks include
  whole steady invocations and prestarted workers with output capacity reserved.
- Initial fixture failures were reproduced and repaired: failed finalize
  leaves configuring state where stop is illegal; queue capacity admits that
  many children; throughput completion includes steals; inactive strict policy
  requests must be absent. Initial test build also exposed GTest dangling-else
  warnings, fixed with explicit braces. None required Runtime or runner changes.
- Clang 14 is absent locally. `apt-get update`: exit 100, environment denies
  setgroups/setegid/seteuid; no privilege workaround attempted. Pinned Clang
  portable assurance and Windows remain hosted gates.

## Claim and remaining-work boundary

Full repository, sanitizer, package and hosted results are pending below as
verification progresses. The existing list regression is a known scope blocker,
not a passing check. API/privacy/package human review is not fabricated.
M23/CAP-M23 remain incomplete; M23-03 through M23-05 and M24-M26 remain future
software scope. No physical CUDA/XDMA, HIL, RT1/RT2, controlled-host latency,
optimality, Unreal, signing, release, deployment or production claim follows.


## Additional observed results

- Full benchmark-enabled build and CTest: build exit 0; CTest exit 8,
  38/39 passed. Only m23_benchmark_artifact_validator failed, exactly at the
  unchanged test_artifacts.py:184 self-only discovery assertion. Its other
  eight adversarial artifact tests passed. No filtering or waiver was applied.
- Dynamic C ABI check against build/m23-cpu/librtfw.so: exit 0, 70 symbols and
  fingerprint 0xd0e7a5a14bf35f97. readelf confirms SONAME librtfw.so.8.
- Current docs/release contract, whitespace and scope checks: exit 0. Changed
  paths are within the active contract; no forbidden path changed.
- A subsequent worker-start assertion incorrectly treated a late worker-loop
  counter increment as new thread creation during step. It failed lifecycle-4
  and memory-normal-256 under sanitizer timing. Source inspection shows worker
  threads are created in Executor::start, while worker_starts increments later
  at worker_loop entry. The fixture now checks the observed count stays within
  the declared team, without assuming immediate visibility. No Runtime change.
- ASan/UBSan build: exit 0. A LeakSanitizer-enabled attempt failed before workload
  because /proc thread inspection is unavailable (fatal ptrace/proc diagnostic).
  This is retained as blocked LSan evidence; the separate ASan/UBSan run uses
  the existing hosted matrix setting detect_leaks=0 and does not claim LSan.


## Focused final-source validation

- Corrected final-source focused CTest: exit 0, 4/4 passed, including all
  26 C++ benchmark tests, CPU allocation gate, exact 51-case inventory and
  CPU consumer. Malformed ordinals leave invocation state unchanged.
- Debug ASan+UBSan CPU CLI/inventory with
  `ASAN_OPTIONS=abort_on_error=1:detect_leaks=0` and
  `UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1`: exit 0, all 51 cases,
  including both native cases. Separate LeakSanitizer block remains above.
- Debug TSan CPU CLI/inventory with `TSAN_OPTIONS=halt_on_error=1`: exit 0,
  all 51 cases, including both native cases. Concurrent-owner focused TSan
  test is recorded separately when complete.
- CPU package wrapper: exit 0. The unchanged M23-01 gate and all additional
  CPU source install, non-default-prefix, ZIP relocation, unrequested import,
  independent embedding and example checks passed. Installed consumer CTest
  ran 19/19 successfully. Final source/digest refresh follows separately.
- Portable assurance `all --build-dir build/m23-portable-assurance
  --source-commit b52b42a6c64066553ce55fb08c874061d78e036e`: exit 1 after
  dependency verification, at the missing Clang 14 tool precondition. Static,
  fuzz and artifact assurance are unperformed locally, not passed.
- First official full profile stopped in quick CTest: 31/33 passed; the two
  thread-policy tests were NOT RUN because a stale generated binary had no
  executable bits. That generated binary is removed and relinked by CMake;
  no source, test or gate changes are used for the repair. Full rerun follows.

- Final installed-source refresh: rebuild optional SDK, install into a new
  prefix, ZIP archive it, remove original prefix, extract elsewhere, reconfigure
  and run all 19 package tests plus standalone installed and embedded CPU
  examples: exit 0. Installed provider source bytes equal final worktree bytes;
  descriptor/raw/result from source and relocated consumers are byte-identical
  and both pass the unchanged validator. No machine observations were replaced.
- Focused TSan: compile tests/test_benchmark_cpu.cpp with -fsanitize=thread and
  link the instrumented CPU, runner and Runtime libraries (prebuilt GoogleTest
  harness); run `TSAN_OPTIONS=halt_on_error=1 build/m23-tsan/cpu-focused
  --gtest_filter=BenchmarkCpu.*`: exit 0, 7/7, including joined concurrent owners.
