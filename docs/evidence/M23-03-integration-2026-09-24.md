# M23-03 integration after device wake repair

Initial integration checkpoint (superseded by the results below): benchmark implementation remains the 87-case source
from PR #254 head `e28a926aa115dcb19b46451f69bf16d806e91ac8`, now integrated
with main `49443ff1cee0966c228300530a93917cc95b81e0`. Fresh combined-source
verification is pending; PR #254's final ledger records results and exact
source/tree/CI identities before merge. No earlier result is silently promoted.

Canonical M23-03 control #475 remains unchanged at
`b489a1a32e1ddd84f7213bae053114a2f17e79d7`, blob
`a6e1ea91b9e2b93833ea8992b46fdbe347797ecf`. The local contract binds the
new integrated main. The separate R4 control #482 and target #259 were integrated
first. Benchmark source does not edit Runtime, original CPU provider, framework,
validator, ABI, schemas or previous repair evidence relative to this main.

## Integrated prerequisite and preserved failures

Device wake repair #259 merged at `49443ff1cee0966c228300530a93917cc95b81e0`.
All 32 hosted checks passed at `ea97f91968b9661d83c8b10ba733ef036fb22e8b`,
workflow 36030838286. Its exact tree was
`311bc708d0327f2e5204851744365e1cb9bc865b`, matching local `c283bab8`.
The controlled old-order production-helper test failed and was rescued/joined;
corrected ordering and public repeated-owner coverage passed. Local quick 33/33,
full 37/37, ASan/UBSan 41/41, TSan 41/41, relocated SDK 20/20 and compiled
ABI/SONAME passed. Initial local full attempts encountered generated binaries
with mode 0644: death-test re-exec/later CTests could not start. The exact failed
logs/hashes and ordinary relinking are retained in #259; no tests or permissions
were overridden. The final unchanged full profile passed.

Earlier benchmark evidence remains retained: original 74/78, 76/78, 77/78 and
83/84 checkpoints, initial new-fixture failures, and these final pre-R4 failures:

- Local CLI sweep timed out after 600 seconds on original
  `device-loopback-8-flight1-8` after 48 preceding artifact bundles completed.
- Hosted Clang RelWithDebInfo/FMA ON job 107729073363, run 36027922605,
  timed out in `m23_benchmark_runtime_noalloc` at 600 seconds; other 45 CTests
  passed.
- Hosted Windows RelWithDebInfo job 107729073322, same run, timed out in
  `simcore_all` at 300 seconds. Last test was
  `CrossMatrixDeterminism/SimCoreDeterminism.HashSameAcrossThreadCounts/2ThreadsFmaOff`.
  Its Runtime provider tests and other 45 CTests passed. This experimental stall
  is not attributed to the device wake repair without evidence.

That benchmark head passed 30/32 hosted checks. No functional timeout was waived.
Source ordering establishes the repaired device notification window; attribution
of every historical stall to it is not claimed. Fresh integration must close
the existing gates with unchanged workloads, deadlines and comparisons.

## Required verification at the initial checkpoint

Use GCC 13.3 and the installed CMake path recorded in the completion evidence.
Run the unchanged 87-case provider, 51 no-allocation invokes, independent owners,
complete Runtime CLI artifact sweep, original 51 CPU and 9 legacy artifact tests,
official quick/full profiles with the optional provider enabled, ASan/UBSan and
TSan, complete original-to-CPU-to-Runtime package chain, compiled ABI and every
existing exact-head hosted check. Record interruption/failure separately from
passing execution. Review the acceptance map in the completion evidence against
the final source and exact base-relative allowed-path set.

The provider catalog, counters, callback totals, bounded composition budgets,
synthetic occupancy schedule, package/source ledger and all original oracles
remain unchanged from the completion checkpoint. Independent human review and
physical characterization remain unperformed. No M23/CAP-M23 completion, RT or
hardware qualification, controlled performance threshold, signing or release is
claimed. M23-04/M23-05 remain separate scopes. Rollback reverts the isolated
benchmark changes while preserving the independently merged Runtime repairs.

## Completed local verification and final documentation closure

All local functional gates below pass on source `938c8cfc2dea40e230ec3d7dadcdc5a07b05a176`,
identical tree `57ae604cbaa69462d23dfc97a0fad159fa9dfe89` to published
`5836ad3de86a3eb2f712040ac6bcaaa2bb1048e8`. The documentation closure update
changes only status/evidence and reviewed digests; implementation, tests,
workloads, timeouts, CMake and CI remain identical. Exact final tree/hosted gates
and merge identity belong in PR #254's final integration ledger.

| Gate | Actual result |
| --- | --- |
| Strict optional build and catalog/concurrency tests | Pass; all 87 cases, two warm-ups plus five measured invokes; independent CPU and four-in-flight device owners |
| Complete Runtime CLI artifact sweep | 87/87 pass, 3.73 seconds; original loopback case included, unchanged validator and counter checks |
| Runtime allocation tracking | All 51 declared cases, seven invokes each, pass in 2.27 seconds |
| Original CPU and legacy artifacts | 51/51 CPU cases; native NOT RUN list empty; legacy artifact tests 9/9 |
| Official verification profile | Quick 33/33 and full 37/37 pass, exit 0; full CTest 36.54 seconds |
| GCC ASan/UBSan | All three provider tests / 87 cases / independent owners pass, 71.049 seconds; detect_leaks=0 |
| GCC TSan | Same complete provider suite passes, 182.570 seconds; halt_on_error=1 |
| Installed/archive-relocated SDK | 23/23 consumers pass; optional target remains absent when unrequested |
| Source/embedding examples | Legacy embedding and CPU/Runtime installed plus embedded examples: five separate 1/1 passes |
| Canonical consumer artifacts | Original, CPU and Runtime generated bundles independently validate as structural fixtures |
| Compiled ABI | Both optional and full libraries: v8, 70 exports, fingerprint 0xd0e7a5a14bf35f97; SONAME librtfw.so.8 |

Commands are the completion evidence's strict configure, complete CTest inventory
and `./scripts/agent-verify.sh full`. Sanitizers rebuild `rtfw_benchmark_runtime`
and its Runtime/framework dependencies with the matching sanitizer, then compile
`tests/test_benchmark_runtime.cpp` with C++20, `-O1 -g -pthread`,
`-fno-omit-frame-pointer` and the matching `-fsanitize`, linking those archives
and GTest. `ASAN_OPTIONS=detect_leaks=0 UBSAN_OPTIONS=halt_on_error=1` and
`TSAN_OPTIONS=halt_on_error=1` are used with a 600-second process liveness bound.
Package verification uses the unchanged
`tests/benchmark_fixtures/runtime_consumer/verify_package.py` with fresh
`build/m2303-post-r4-package` and three `m2303-post-r4-*-canonical` outputs.
Logs have prefix `m2303-post-r4-` under the local evidence directory.

The optional CTest invocation first passes 41/42: its cached combined sample
executable was mode 0644 and could not start. Hash
`6c97aa2fbbcd5385edd396191deb7eadc1b0e3a4683152f838055fe0a227c2c8`
and the original log are retained; ordinary relinking restores its separate
1/1 test pass. The first official full attempt passes quick 33/33, full 36/37;
only the cached host-adapter executable cannot start (mode 0644, hash
`13bd91e3f1855c928ff7cd8bb6f2bf4b103f3bc6285cb7ce77feef9e6be1532d`).
Its ordinary relink followed by the unchanged full profile passes 33/33 + 37/37.
No permissions, assertions, deadlines or workload were overridden.

The package wrapper exits 0, but its aggregate log capture ends during legacy
embedding configuration. The six fresh per-build `LastTest.log` files establish
all 23+5 consumer passes, including Runtime embedding at 17:20 UTC; their copied
logs and hashes are retained in `m2303-post-r4-package-evidence.json`. All three
produced canonical bundles independently validate. A truncated aggregate log
alone was not treated as completion.

Agent acceptance review covers the completion evidence's case/test map, public
ownership and checked teardown, whole-invocation timing, independent counter
oracles, lossless nested replay, original workload preservation, finite capacities,
source ledger and package boundaries. Protected Runtime/framework/prior-provider/
validator/ABI sources and earlier repair evidence match integrated main exactly.
No independent human or physical review is inferred. The previously failed CLI
and allocation gates now pass after the causal device repair; no claim is made
that the separate historical experimental Windows stall shared that cause.

## Retained Windows Debug crash and diagnostic continuation

Hosted integration head `5836ad3de86a3eb2f712040ac6bcaaa2bb1048e8`, workflow
36032536793, fails Windows Debug job 107744578788: `simcore_all` reports native
SegFault after 64.91 seconds. Its last test is
`BenchmarkRuntime.EveryCasePreservesWarmupAndMeasuredBoundaries`; the original
GTest scoped trace is not printed on a native crash, so the exact case and stage
are unknown. The other 45 CTests pass, including the separate benchmark suite,
complete CLI inventory and allocation test. Windows RelWithDebInfo job
107744579018 is canceled during build by the existing matrix failure handling;
it is unperformed, not a passing or waived gate.

This new failure is distinct from the pre-R4 Windows experimental determinism
timeout. Local full/ASan/UBSan/TSan and package passes do not erase either failure.
The PR remains draft. A test-only diagnostic now flushes the case identifier and
prepare/invoke/finish/complete stage outside the Runner's timed invocation.
It changes no provider workload, deadline, comparison, counter, Runtime hook or
CI gate. A fresh diagnostic run is intended to identify the causal failure;
a lucky pass alone is not described as a repair. Full-sequence local ASan/UBSan
coverage including preceding experimental tests is also being prepared.
