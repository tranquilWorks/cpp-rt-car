# M23-04 implementation evidence

Status: incomplete implementation checkpoint; not ready to merge.

Baseline: PR #254, `03e4c36b24a7a4a6a2a6083aef26b6f45b1dcd9e`, exact tree
`6b611cd07bcf862142c8b18c821832a4ac9a42ac`. All predecessor 32 checks passed at
`f9796335a7e706bb76f97a79dbe76939a86b8247`, main CI 36037346344. No predecessor
source, test oracle or failed evidence is removed.

Canonical M23-04: control PR #488, merged `49b134215e74b2d6f27ae1cab3801ac9dc8c139d`,
blob `479bbb95244b1ad7817158bf025dcc12568e8af3`. The canonical batch validator,
independent Draft 2020-12 schema validation, path rules, dependency order and
roadmap transition pass. Control CI run 36057177237 / job 107826973171 reports
failure before any steps (steps null), with no log URL and BlobNotFound on log
retrieval. It is retained as unperformed under the standing unavailable-hosted-job
exception, not passed. Full portfolio-wide local validation was unperformed:
private Git clone was unavailable; unchanged canonical validation modules were
retrieved with the authorized connector. No functional target gate is waived.

## Implemented checkpoint

- A separately owned `rtfw.device` provider uses actual CudaDeviceBackend public
  APIs with benchmark-owned injected driver storage. Metadata does not probe a
  device or initialize a backend.
- Whole transactions cover H2D/D2H roundtrip, D2D with roundtrip validation,
  memset and increment kernel at 64/4096 bytes; actual queue occupancy 1/4,
  reject-next and recovery, empty/not-ready poll and timeout quarantine.
- Lifecycle cases include allocation/setup, failed submit/reset/recovery,
  context loss, retained resource ownership and checked cleanup retry. They
  are explicitly excluded from steady allocation-free descriptors.
- Eight real-session descriptors return zero-sample NOT RUN with no session.
  A supplied-session test proves actual injected driver/kernel calls and
  exact elementwise output; this is simulated protocol evidence only.
- An opt-in CUDA host example owns a real context/stream/PTX module through
  checked backend shutdown, but toolkit compilation and physical execution
  are not yet performed. The PTX transform follows the existing retained
  qualification sample without modifying it.
- CLI discovery adds the exact fourth provider while retaining the complete
  previous inventory and its independent assertions.

## Executed checks and retained failure

Environment: GCC 13.3, CMake 4.4.3, Linux. No NVIDIA/XDMA device nodes observed.

1. `cmake -S . -B build/m23-device -DCMAKE_BUILD_TYPE=RelWithDebInfo
   -DENABLE_TESTS=ON -DRTFW_BUILD_EXPERIMENTAL=OFF -DRTFW_BUILD_BENCHMARKS=ON
   -DSIM_SANITIZERS= -DSIM_WERROR=ON` — pass.
2. `cmake --build build/m23-device --target simcore_tests rtfw-bench --parallel 2`
   — pass, strict warnings enabled.
3. Initial `BenchmarkDevice.*` run: 3 pass, 1 fails provider registration.
   Descriptor configuration contained `=` and `;`, forbidden by the existing
   identifier validator. Corrected the descriptor to `bytes-N-depth-N`; no
   framework/schema/validator change. The subsequent complete 4-test run passes.
4. `build/m23-device/tests/simcore_tests --gtest_filter=BenchmarkDevice.*`
   — 4/4 pass after adding lifecycle faults. Includes every current catalog case,
   supplied-session kernel execution, observed full occupancy/rejection and
   malformed supplied driver cleanup.
5. `python3 tests/benchmark_fixtures/test_device_cases.py --cli
   build/m23-device/bench/rtfw-bench --inventory bench/fixtures/device_cases.json`
   — 24/24 pass through actual CLI and unchanged offline validator; 16 execute,
   8 valid NOT RUN. Independently checks observed counters, integer checksums,
   warmup/measured conservation and destination preservation.
6. `git diff --check` — pass at this checkpoint.

Logs: `/tmp/m2304-configure.log`, `/tmp/m2304-build.log`, initial failure
`/tmp/m2304-cuda-tests.log`, corrected `/tmp/m2304-cuda-fix-tests.log`, and
`/tmp/m2304-fault-{build,tests,cli}.log`. These transient logs are summarized
here so pruning does not erase their disposition.

## Remaining acceptance

CUDA Graph/native HAL, XDMA operations/capacity/faults and optional real session,
host staging and full Runtime combined pipeline, complete real host examples,
optional installed/embedded/relocated consumers and exact release digests,
allocation/concurrency/sanitizers, static/CI/canonical artifact exchange, full
local profiles and final review remain pending. This checkpoint does not satisfy
the complete M23-04 acceptance contract. M23-05 and physical characterization
remain separate. Existing Runtime/drivers/ABIs/providers are preserved.

No physical, controlled-performance, support, RT, human-review, signing, release
or deployment result is inferred. No cybersecurity, Daybreak or Unreal scope.
