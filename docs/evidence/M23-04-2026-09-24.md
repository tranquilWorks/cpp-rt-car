# M23-04 implementation evidence

Status: retained implementation and verification history. The initial checkpoint
was incomplete; later sections record the 68-case software candidate. Final
exact-head integration is recorded in PR #260.

Baseline: PR #254, `03e4c36b24a7a4a6a2a6083aef26b6f45b1dcd9e`, exact tree
`6b611cd07bcf862142c8b18c821832a4ac9a42ac`. All predecessor 32 checks passed at
`f9796335a7e706bb76f97a79dbe76939a86b8247`, main CI 36037346344. No predecessor
source, test oracle or failed evidence is removed.

Canonical M23-04: control PR #488, merged `49b134215e74b2d6f27ae1cab3801ac9dc8c139d`,
blob `479bbb95244b1ad7817158bf025dcc12568e8af3`. The canonical batch validator,
independent Draft 2020-12 schema validation, path rules, dependency order and
roadmap transition pass. Control CI run 36057177237 / job 107826973171 reports
failure before any steps (steps null), with no log URL and BlobNotFound on log
retrieval. It is retained as unperformed under the standing unavailable-hosted-job
exception, not passed. Full portfolio-wide local validation was unperformed:
private Git clone was unavailable; unchanged canonical validation modules were
retrieved with the authorized connector. No functional target gate is waived.

## Implemented checkpoint

- A separately owned `rtfw.device` provider uses actual CudaDeviceBackend public
  APIs with benchmark-owned injected driver storage. Metadata does not probe a
  device or initialize a backend.
- Whole transactions cover H2D/D2H roundtrip, D2D with roundtrip validation,
  memset and increment kernel at 64/4096 bytes; actual queue occupancy 1/4,
  reject-next and recovery, empty/not-ready poll and timeout quarantine.
- Lifecycle cases include allocation/setup, failed submit/reset/recovery,
  context loss, retained resource ownership and checked cleanup retry. They
  are explicitly excluded from steady allocation-free descriptors.
- Eight real-session descriptors return zero-sample NOT RUN with no session.
  A supplied-session test proves actual injected driver/kernel calls and
  exact elementwise output; this is simulated protocol evidence only.
- An opt-in CUDA host example owns a real context/stream/PTX module through
  checked backend shutdown, but toolkit compilation and physical execution
  are not yet performed. The PTX transform follows the existing retained
  qualification sample without modifying it.
- CLI discovery adds the exact fourth provider while retaining the complete
  previous inventory and its independent assertions.

## Executed checks and retained failure

Environment: GCC 13.3, CMake 4.4.3, Linux. No NVIDIA/XDMA device nodes observed.

1. `cmake -S . -B build/m23-device -DCMAKE_BUILD_TYPE=RelWithDebInfo
   -DENABLE_TESTS=ON -DRTFW_BUILD_EXPERIMENTAL=OFF -DRTFW_BUILD_BENCHMARKS=ON
   -DSIM_SANITIZERS= -DSIM_WERROR=ON` — pass.
2. `cmake --build build/m23-device --target simcore_tests rtfw-bench --parallel 2`
   — pass, strict warnings enabled.
3. Initial `BenchmarkDevice.*` run: 3 pass, 1 fails provider registration.
   Descriptor configuration contained `=` and `;`, forbidden by the existing
   identifier validator. Corrected the descriptor to `bytes-N-depth-N`; no
   framework/schema/validator change. The subsequent complete 4-test run passes.
4. `build/m23-device/tests/simcore_tests --gtest_filter=BenchmarkDevice.*`
   — 4/4 pass after adding lifecycle faults. Includes every current catalog case,
   supplied-session kernel execution, observed full occupancy/rejection and
   malformed supplied driver cleanup.
5. `python3 tests/benchmark_fixtures/test_device_cases.py --cli
   build/m23-device/bench/rtfw-bench --inventory bench/fixtures/device_cases.json`
   — 24/24 pass through actual CLI and unchanged offline validator; 16 execute,
   8 valid NOT RUN. Independently checks observed counters, integer checksums,
   warmup/measured conservation and destination preservation.
6. `git diff --check` — pass at this checkpoint.

Logs: `/tmp/m2304-configure.log`, `/tmp/m2304-build.log`, initial failure
`/tmp/m2304-cuda-tests.log`, corrected `/tmp/m2304-cuda-fix-tests.log`, and
`/tmp/m2304-fault-{build,tests,cli}.log`. These transient logs are summarized
here so pruning does not erase their disposition.

## Remaining acceptance at the initial checkpoint

CUDA Graph/native HAL, XDMA operations/capacity/faults and optional real session,
host staging and full Runtime combined pipeline, complete real host examples,
optional installed/embedded/relocated consumers and exact release digests,
allocation/concurrency/sanitizers, static/CI/canonical artifact exchange, full
local profiles and final review remain pending. This checkpoint does not satisfy
the complete M23-04 acceptance contract. M23-05 and physical characterization
remain separate. Existing Runtime/drivers/ABIs/providers are preserved.

No physical, controlled-performance, support, RT, human-review, signing, release
or deployment result is inferred. No cybersecurity, Daybreak or Unreal scope.

## Device and pipeline integration checkpoint

The catalog now has 52 cases (40 portable, 12 real-session descriptors). Added
actual XDMA I/O-worker transfers/occupancy, control/event operations, short I/O,
loss, reset recovery, event timeout/cancellation and retained cleanup retry.
Native CUDA graph cases execute the increment transform over caller-bound device
storage and verify H/G/D/event order, exact timeline identity and output. Full
Runtime kernel and graph pipelines at 64/4096 bytes execute one/four frames with
one/four CPU workers, disjoint host staging and one/two XDMA workers. Runtime
owns both backend lifetimes. Pipeline output and backend health deltas are checked.

Executed locally with strict GCC 13.3:
- 8/8 focused GTests pass, including all catalog cases, supplied CUDA graph and
  XDMA driver/offset execution, invalid supplied resources, corrupt graph output
  and independent concurrent owners.
- 52/52 CLI bundles pass unchanged artifact validation and independent counter/
  checksum oracles; 12 missing real sessions produce NOT RUN with zero samples.
- The device allocation guard passes every declared steady portable case,
  including Runtime pipeline invokes and prestarted backend workers.
- Native Linux XDMA host target compiles. Its explicit endpoint/window interface
  has no guessed card map. Native CUDA graph host code is written but still
  awaits toolkit compilation. No physical execution has occurred.
- Optional source install, relocated/embedded wrapper, canonical consumer and
  additive CI artifact/static gates are integrated; full package verification
  is running and not yet claimed passed.

Retained failures:
- First target CI run 36059158073 failed relocated jobs 107833570625,
  107833570418, 107833570255; docs job 107833569526 in run 36059158108 also failed.
  Actual logs identify stale seven source digests after additive edits. The
  source release ledger was updated; the unchanged release semantics and local
  checker now pass. These hosted failures are not waived or relabeled passed.
- First full pipeline fault run aborted on checked cleanup. The fixture omitted
  Runtime's required reset after a failed graph submission retained live work.
  Added the existing public `reset_device` before stop, as in the retained
  combined sample contract. No Runtime/backend change. Subsequent full catalog,
  CLI and allocation runs pass. Original failure log:
  `/tmp/m2304-pipeline-tests.log`; diagnostic stop status -2, retained ownership.

Current local logs: `/tmp/m2304-pipeline-fix-{build,tests}.log`,
`/tmp/m2304-pipeline-noalloc.log`, `/tmp/m2304-pipeline-cli.log`,
`/tmp/m2304-xdma-native-{configure,build}.log`, `/tmp/m2304-package.log`.

Still incomplete: remaining HAL rejection/metadata coverage, standalone staging,
real combined sessions and complete host integration, toolkit compilation, full
sanitizers/static/CI/package closure, final acceptance mapping and exact-head
verification. PR #260 remains draft; hardware characterization remains NOT RUN.

The first package-wrapper run failed at consumer configuration: an additive edit
inserted the new executable twice (including before find_package). Removed the
early duplicate; the original component request and final executable block are
retained. A clean wrapper rerun is required. Initial log `/tmp/m2304-package.log`.

## Software verification candidate (68 cases)

Added eight HAL v1/v2 metadata/topology cases, two independent staging cases,
two malformed/stale-input lifecycle cases and four functional supplied-session
Runtime pipeline descriptors. The catalog is 68 cases (52 portable, 16 default
NOT RUN), within the unchanged registry bound of 256 (207 total providers/cases).
Pipeline descriptor fields distinguish frame/worker counts from queue capacity.
Real CUDA, XDMA and composed host examples are implemented; the composed host
owns both adapters and a confirmed AXI-MM window through checked Runtime stop.
The driver identity explicitly distinguishes benchmark protocol storage.

The original package wrapper, CPU and Runtime wrappers, and new device wrapper
all passed in `build/m23-device-package-v2`: optional install/archive relocation,
unrequested-component isolation, installed source and embedded consumers. Later
source additions require a final refreshed install check. Source-example canonical
CUDA graph bytes avoid nondeterministic I/O worker polling counts.

Local full strict build succeeded. First full CTest attempt: 24 passed; 20
simcore invocations could not start because the generated executable lacked its
execute bit; the dependency gate also reported an uninitialized GoogleTest
submodule. The actual ELF was inspected, its generated execute bit restored,
and the exact pinned submodule initialized. All 21 failed/not-run entries then
passed, including simcore_all. A subsequent ordinary relink produced an
executable with the normal mode. No test expectation or source gate was weakened.
Logs: `/tmp/m2304-full-build.log`, `/tmp/m2304-full-ctest.log`,
`/tmp/m2304-full-ctest-retry.log`, `/tmp/m2304-submodule.log`.

CUDA 12.9.79 headers initially rejected the legacy five-argument graph instantiate
call. Using the public `cuGraphInstantiateWithFlags` API fixes compilation against
those headers; strict syntax-only compilation of both CUDA and composed host
sources passes. This is compilation evidence, not link or physical execution.
Logs: `/tmp/m2304-cuda-syntax.log`, `/tmp/m2304-native-syntax.log`.

The optional native XDMA missing-node script passes and confirms a valid NOT RUN
bundle, exit 3, and destination preservation. Its first ad hoc harness treated
validator exit 3 as an error even though the validator reported valid NOT RUN;
the committed harness checks the documented not-run exit status explicitly.

First HAL metadata sweep failed preparation of XDMA because the fixture reduced
max buffer bytes to 4096 but retained a larger default max transfer. Declaring
both limits as 4096 fixes the invalid configuration. Subsequent 68-case CLI and
focused tests pass. Original log `/tmp/m2304-hal-tests.log`; corrected
`/tmp/m2304-invalid-{build,tests,cli}.log`.

Hosted intermediate head `559939be96fbd6a577723aa2cae1ef5797bb157d`, exact tree
`3c8afc251740012992d2214574959a544171f52b`, passed the GCC/Clang/MSVC optional SDK
jobs, native toolkit/node-free adapter consumers, seven-way deterministic device
exchange, TSan's existing filter, docs and relocated consumers (CI 36061822110,
docs 36061822161). This was the 52-case checkpoint, not the final 68-case source.
The final TSan and focused benchmark filters now explicitly add BenchmarkDevice.*;
no final-head result is inferred from that older run.

The new foreign-owner Runtime regression initially expected `invalid_argument`.
The public Runtime contract returns the more specific `invalid_handle` for an
owner mismatch. Confirmed against its retained implementation and corrected only
the new test oracle; all payload/no-driver-activity/recovery assertions remain.
Initial run: `/tmp/m2304-final-device-tests.log` (10 pass, 1 expectation failure).

## Final local device gates

- Latest strict device build and focused CTest gate pass: package consumer,
  allocation guard, 68-case CLI/steady/bounded-stack suite and the complete
  existing benchmark suite with BenchmarkDevice included (4/4 CTest entries).
  Eleven focused device GTests pass, including foreign-handle recovery.
  `/tmp/m2304-complete-{build,device}.log`, `/tmp/m2304-foreign-fix-tests.log`.
- Final optional wrapper chain passes from a fresh installation in
  `build/m23-device-package-final`: original/CPU/Runtime/device consumers,
  archive relocation, component isolation, installed source and embedding.
  `/tmp/m2304-package-final.log`.
- Strict GCC 13.3 ASan+UBSan build of device GTests and allocation consumer passes;
  11/11 tests and the complete steady allocation sweep pass with
  `ASAN_OPTIONS=detect_leaks=0:abort_on_error=1` and
  `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1`, matching the existing CI
  leak-scanning policy. `/tmp/m2304-asan-final-{build,tests,noalloc}.log`.
- Initial leak-enabled sanitizer runs passed all 11 tests but failed process exit
  because LeakSanitizer cannot inspect this traced execution environment
  (`LeakSanitizer does not work under ptrace`). This is an unavailable leak scan,
  not a passed leak check; the failure is retained in
  `/tmp/m2304-asan-device-tests.log` and `/tmp/m2304-asan-noalloc.log`.
- The GCC 13 sanitized CLI build was blocked by `-Werror=array-bounds` in the
  unchanged self-case vector initializer at `benchmark_cli.cpp:24`. No warning
  suppression, framework change or altered self-case workload was introduced.
  Local normal CLI gates pass; sanitized CLI compilation on the supported named
  GCC/Clang CI tuples remains a required final-head gate. Original diagnostic:
  `/tmp/m2304-asan-build.log`.
- Source ABI and compiled `build/m23-device/librtfw.so` checks pass with 70 v8
  symbols and fingerprint `0xd0e7a5a14bf35f97`. Documentation/release checks and
  the complete contract profile pass. The full diff is restricted to
  allowed paths; no protected Runtime/driver/framework/provider source changes.
- The cumulative agent full profile passed its contract stage and quick 33/33
  tests plus runtime-demo behavior; its experimental full stage is still running
  at this checkpoint. Final disposition is recorded in PR #260.

Agent host-path review found that existence alone could label a regular file as
an XDMA endpoint. Native XDMA/composed hosts now reject existing non-character
paths before opening anything, even when the other endpoint is absent. The
node-free harness checks exit 2, no output artifact and unchanged input bytes.
This is configuration/identity validation; no backend or driver behavior changed.
