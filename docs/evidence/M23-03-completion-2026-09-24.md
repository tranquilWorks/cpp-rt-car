# M23-03 benchmark acceptance completion

Status: implementation complete; final integration verification in progress in
PR #254. This checkpoint does not claim a merge or any pending gate as passed.

Canonical amendment: control #475, revision
`b489a1a32e1ddd84f7213bae053114a2f17e79d7`, blob
`a6e1ea91b9e2b93833ea8992b46fdbe347797ecf`. Integrated baseline:
`8ba51b3effa6174eaeb5382aef3404eeb5c160d1` (watchdog repair #258), after
queue #257 and lossless replay #256. Repair #258 passed all 32 exact-head checks
at `a520626bd0fa024afb54f3fe2270669acf1db562`, workflow 36021280017; local
quick 33/33/full 37/37, ASan+UBSan 36/36, TSan 36/36, relocated SDK 20/20,
C ABI v8 / 70 exports / fingerprint 0xd0e7a5a14bf35f97 / SONAME 8, and the
unchanged default-v1 golden passed. Its original full-suite timeout is retained
in #258, including the single controlled serialized pass; no causal fix for
that timeout is claimed. Original benchmark 74/78, 76/78, 77/78 and 83/84
checkpoints remain in the earlier evidence and published history.

## Delivered scope and acceptance map

The sorted catalog has 87 cases, five parameters and twelve counters, within
the approved 96/16/16 limits. All original 78 workload geometries remain.

| Acceptance group | Implementation and observations |
| --- | --- |
| Discovery, timing, ownership, package boundary | Instance-local public `rtfw.runtime` source provider; metadata-only discovery; unchanged runner/validator/schemas; selected preparation and checked cleanup; installed examples only. All original self/51 CPU inventory and package wrappers are retained. |
| CPU rates and channels | Integer release/substep/state oracles; first/repeated selection windows; actual publish/copy counts; initial/held/stale provenance; 256 channels; 65536 reference records. |
| Device rates and nonpublication | Original eight pipeline cases plus error/timeout lifecycle checks. New `device-inflight-configured-4` observes four actual pending loopback submissions, four completions/providers/payload copies and zero remaining ownership. |
| Shedding and controls | Original thresholds 1/3 plus separate public action/counter reads. Exact accepted/invalid/full/stale deltas, invalid host/rate target, copied values, replacement, rollback and stop. Separate accepted-admission column. |
| Checkpoint and replay | Exact/short writes, registered state and 64-input capacities, reject-next; corrupt/truncated/foreign identity rejection before mutation; complete input/state/action/generation oracles. Composition explicitly opts into bounded v2 retained admissions. |
| Watchdog and observability | Disabled/below/expired and cap 1/3; prior callback degradation; trace 0/16/1024, interval/foreign cursor, short sink noncommit/recovery; distinct rate/mixed/control action streams and configured 1024-record overflow. |
| Capacity boundaries | Full configured dispatch/control/trace/action occupancy; domain/substep reject-next; inclusive 262144 compiled selections and rejection at 262656; public inclusive/reject-next action and 1 GiB configuration caps without large allocation. |
| Composition | Original four cases plus `composition-shedding-2/-8`, with explicit mandatory late trigger, optional shedding/recovery, invalid control, watchdog and full nested replay closure. Callback totals include all forward/replay producers/providers/consumers/optional/trigger work. |
| Integration | Default source ledger restored to target/source format; all ten new optional translation units routed through the existing optional analyzer. TSan filter adds Runtime provider and mixed-rate replay while preserving prior patterns. Seven Runtime canonical bundles join the unchanged original and CPU compiler/FMA/MSVC exchange. |

The occupancy fixture uses the installed loopback for every command and payload
copy. Its small borrowed public command-extension adapter delays completion
observation until four accepted submissions are pending. It returns immediately
from polls, adds no thread or sleep, and enables draining during cancellation or
stop. Four phases share one rate release, so Runtime submits them before the
release's completion barrier. Atomic adapter counters remain instance-local;
concurrent isolation now exercises two such owners as well as two CPU owners.
This declared synthetic schedule is not native hardware latency.

New shedding variants register the mandatory trigger before the two optional
domains. Its 3 ns declared deadline admits preceding unit CPU budgets; injected
logical work makes the first release late. Public transitions alternate shed and
recover for optional domain index 5. Three on-time mandatory releases recover it
before the next optional callbacks. Every forward/replayed frame observes two
optional callbacks and one trigger callback. Original workloads are preserved.

Two-instance replay/composition/checkpoint fixtures split the declared Runtime
budget 480/32 MiB, totaling 512 MiB. The foreign checkpoint owner exists only in
prepare. Borrowed callback/backend/buffer/clock storage outlives checked stop.
All callback bodies remain bounded and allocation-free by inspection; 51 complete
steady invoke cases also run with global allocation tracking. Host artifact
creation/restoration/export remains disclosed work inside the timed invocation.
The peak-outstanding counter is a per-invocation observation; summing its samples
does not produce a run-wide peak. No unlike operation units are added together.

## Verification commands and checkpoint results

Tool path: `/workspace/scratch/6fa6fd555f66/build-tools/cmake/data/bin`.
Local GCC 13.3; Linux x86-64. Configure-time dirty identity is retained for
prepublication checks. Final clean source/tree and exact CI identities belong in
PR #254's integration ledger to avoid a self-referential source hash.

```sh
cmake -S . -B build/m23-runtime -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DENABLE_TESTS=ON -DRTFW_BUILD_EXPERIMENTAL=OFF \
  -DRTFW_BUILD_BENCHMARKS=ON -DSIM_SANITIZERS= -DSIM_WERROR=ON
cmake --build build/m23-runtime --parallel 2
build/m23-runtime/tests/simcore_tests \
  --gtest_filter='BenchmarkRuntime.*:BenchmarkCpu.*:BenchmarkContract.*:BenchmarkRunner.*:BenchmarkArtifacts.*'
build/m23-runtime/bench/rtfw_benchmark_runtime_noalloc
python3 tests/benchmark_fixtures/test_runtime_cases.py \
  --cli build/m23-runtime/bench/rtfw-bench --inventory bench/fixtures/runtime_cases.json
python3 tests/benchmark_fixtures/test_cpu_cases.py \
  --cli build/m23-runtime/bench/rtfw-bench --inventory bench/fixtures/cpu_cases.json
python3 tests/benchmark_fixtures/test_artifacts.py \
  --cli build/m23-runtime/bench/rtfw-bench \
  --consumer build/m23-runtime/bench/rtfw_benchmark_consumer
```

Initial strict build passes. Focused non-experimental suite passes 15/15,
including every catalog case for two warm-ups and five measured invokes, and
CPU/device concurrent ownership. All 51 allocation-tracked cases pass seven
invocations. Final artifact inventory, optional/full CTest, sanitizer, package,
ABI and exact-head hosted results remain pending at this checkpoint.

## Retained development failures

- First new CLI invocations used a parent-component output path; destination
  validation rejected it before fixture preparation. Absolute destinations fix
  the command, without a publication-policy change.
- Initial occupancy setup called `hal_v2_registration` before `add_route`;
  registration freezes routes. Configure the adapter after adding the route.
- Initial signal equaled the timeline's initial value; monotonically increasing
  signals now begin at initial+1, preserving timeline ownership.
- The first four-phase occupancy attempt used four separate rate domains. Runtime
  drains each domain release before advancing, so the four-completion gate could
  never open and the bounded diagnostic timed out at 60 seconds. All four
  independent phases now bind to the same domain release, matching the intended
  concurrent workload. No Runtime waiting or timeout policy is changed.
- A one-nanosecond trigger deadline failed declared-budget admission after two
  unit-budget CPU records. The new fixture uses the feasible 3 ns deadline.
  Registering it after optional domains recovered too late for both callbacks;
  the explicit intended ordering is now registered before those optional domains.
  Public action/counter assertions retain the planned two-callback oracle.
- Earlier hosted run 36019658309 failed the optional source manifest with
  `expected target|source`: optional sources are absent from the default compile
  database. They now use the existing explicit optional analyzer instead of
  inventing default targets. Earlier Clang source-location compatibility and
  composition replay failures remain in the continuation ledger.

No independent human review is claimed. No hardware is accessible from this
workspace; no physical/RT qualification or performance threshold is established.
M23/CAP-M23 remain incomplete pending M23-04/M23-05. Scope excludes Unreal,
cybersecurity, Daybreak, support promotion, signing, release and deployment.
Rollback is the isolated provider/CLI/catalog/integration change; merged Runtime
repairs remain independently usable.
