# M23-04 acceptance map

Software verification candidate; final exact-head CI and merge disposition are
recorded in PR #260. Retained failures and checkpoint chronology are in
[M23-04 implementation evidence](M23-04-2026-09-24.md).

Canonical scope: portfolio-control #488, merge
`49b134215e74b2d6f27ae1cab3801ac9dc8c139d`, blob
`479bbb95244b1ad7817158bf025dcc12568e8af3`. Target baseline:
`03e4c36b24a7a4a6a2a6083aef26b6f45b1dcd9e` (merged #254).

| Acceptance | Implementation and independent verification |
| --- | --- |
| Finite separate provider and retained inventory | 68 `rtfw.device` descriptors; 16 counters maximum, four parameters maximum. Exact combined inventory 207 includes unchanged 87 Runtime, 51 CPU and one self case. All original list expectations are extended rather than replaced. |
| Public backend/HAL APIs | Actual CudaDeviceBackend and XdmaDeviceBackend, native HAL v2 and legacy v1 tables; Runtime also exercises native pipelines and rejects a foreign owner through legacy adapted registration. No private Runtime/backend imports. |
| Discovery and capability inspection | Eight `hal-{cuda,xdma}-v{1,2}-depth-{1,4}` cases inspect actual capacities; native cases inspect topology and command capabilities. Assert no driver allocation, events, initialization or transfer activity. Describing providers performs no preparation. |
| Preparation and timing scope | Separate checked prepare/invoke/finish; complete transactions include completion and output validation. Lifecycle descriptors include construction/allocation/reset/cleanup. No submit-only or subtracted work claim. |
| CUDA transfers/transforms | Roundtrip, D2D, memset, kernel and native graph at 64/4096 bytes. Graph binds ID 7 to named retained storage; callback actually increments bytes, validates H/G/D/event order, ticket/signal identity, empty re-poll and independent output oracle. |
| Occupancy and poll behavior | CUDA/XDMA depths 1/4 hold work pending, observe full backend health, reject extra submission, terminalize unique IDs and prove fresh work. Empty/not-ready CUDA polling and timeout quarantine are separate cases. |
| CUDA fault ownership | Submit failure/reset, context loss, cleanup retry and graph launch failure through Runtime. No successful output or copy count for failed transactions; cleanup retains potentially live storage until checked drain/reset/stop. |
| XDMA operations and I/O workers | Actual asynchronous H2C/C2H payloads at 64/4096 bytes, control write/read and user-event output, exact completion bytes/status, worker ownership and bounded poll deadlines. |
| XDMA faults | Short transfer changes only the transferred prefix; error/loss/reset-required, timeout, stop cancellation and shutdown retry. Reset proves a subsequent complete roundtrip. Timeout/cancel leave host output unchanged. |
| Invalid/stale/foreign inputs | CUDA oversized/access/stale references; XDMA channel/device-offset overflow/range/access/stale rejection. Independent Runtime foreign-handle test proves specific `invalid_handle`, unchanged host bytes, no driver activity and valid-owner recovery. Invalid supplied real tables/configuration are errors. |
| Explicit host staging | Disjoint 64/4096-byte source/destination, independently checked byte pattern and untouched tail. Runtime bridge checks transformed CUDA output before copying to separate XDMA storage. |
| Complete Runtime pipelines | Kernel/graph × 64/4096 × one/four frames and CPU workers, two backend-local timelines, exact independent integer output, backend health submission/completion deltas and actual fake-driver transform/transfer counts. CUDA failure suppresses every downstream output and uses public reset before stop. |
| Bounds and independent ownership | Heap-owned fixed fixture storage; 240 MiB declared Runtime budget per pipeline; two concurrent owners total 480 MiB, at most four CPU/two XDMA workers each, one CUDA/one XDMA backend each. Concurrent direct and full Runtime instances have independent drivers, storage and ordinals. |
| Steady allocation and caller stack | Global allocation guard covers every declared steady portable descriptor and all prestarted threads; separate lifecycle flags exclude reconstruction. CLI probes four direct/lifecycle/pipeline cases with a 512 KiB stack; original Runtime probes are retained. |
| Functional supplied real sessions | CUDA kernel/graph and XDMA confirmed-offset tests invoke injected supplied tables and inspect actual bytes/calls/resource conservation. Combined supplied kernel/graph pipelines invoke both drivers through Runtime. Corrupt supplied graph output poisons the run until finish. Evidence is protocol simulation, not physical. |
| Production host examples | CUDA, Linux XDMA and composed executables own adapters/resources through checked finish. Explicit endpoint/window arguments, no guessed register map, graph instance/device span retained. Optional toolkit job compiles/links all hosts; node-free native XDMA check verifies NOT RUN and destination preservation. |
| Missing resources | All 16 real descriptors in ordinary CLI produce valid zero-sample NOT RUN and exit 3 before warmup. Missing prerequisites differ from invalid supplied configuration/runtime failure. No fallback to a fake session. |
| Artifact/identity boundaries | Unchanged v1 schemas and offline validator; actual fake/supplied implementation identity and explicit driver identity. Full raw artifacts, independent per-sample counter/checksum formulas, retained destinations; no successful publication after failed cleanup. |
| Package integration | New device wrapper chains unchanged Runtime → CPU → legacy wrappers. Optional source ledger, installed/relocated/unrequested/embedded consumers. No added default provider target/header or stable ABI. Exact release digests cover new installed files. |
| Retained verification gates | Existing compiler/FMA/MSVC, ASan/UBSan, TSan, package/static/ABI gates remain. Device TU includes its graph/pipeline/HAL fixtures in optional static analysis. Device tests are added to focused benchmark and TSan filters. Canonical fake graph bundle joins all seven deterministic exchanges without worker-poll normalization. |

Local outcomes, exact commands and initial failures are retained in the companion
evidence. Candidate changes preserve Runtime, drivers, public framework headers
and implementation, schemas/validator, CPU/Runtime provider sources, existing
backend tests, ABI v8, 70 exports, fingerprint `0xd0e7a5a14bf35f97`, SONAME 8,
release version, license, support matrices and prior repair/evidence history.

Review status: agent self-review of timing, ownership, counter meaning, resource
bounds, source scope and software/physical evidence boundaries. This is not an
independent human review; no such review is claimed.

Physical hardware: NOT RUN. No accessible NVIDIA/XDMA bench was present. No
controlled-performance, physical CUDA/FPGA support, M18, RT qualification, signing,
release or deployment result follows from these checks. M23/CAP-M23 remain open
for the separately planned M23-05; no other milestone scope is included here.
